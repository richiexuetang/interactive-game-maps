// adjust this per your needs
// (7 days * 24 hours * 60 minutes * 60 seconds * 1000 milliseconds)
export const expiresTimeTokenMilliseconds = 7 * 24 * 60 * 60 * 1000;

export const COOKIE_NAMES = {
  JWT: "jwt",
};
